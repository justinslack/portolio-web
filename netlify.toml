# Netlify configuration for Next.js deployment

[build]
  command = "pnpm build"
  publish = ".next"

# Note: NEXT_PUBLIC_* variables are intentionally public and bundled into client-side JS
# If deployment fails due to "exposed secrets", mark these as non-sensitive in Netlify dashboard:
# Site settings → Environment variables → Mark as non-sensitive

# Install Next.js plugin for Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Headers for security and caching
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache static assets
[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache images
[[headers]]
  for = "/_next/image/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Environment variables documentation
# Add these in Netlify Dashboard -> Site settings -> Environment variables
# Required for frontend:
# - NEXT_PUBLIC_ALGOLIA_APP_ID
# - NEXT_PUBLIC_ALGOLIA_SEARCH_API_KEY
#
# Optional (if running indexing script on Netlify):
# - ALGOLIA_ADMIN_API_KEY
# - DISCOGS_OAUTH_TOKEN
# - DISCOGS_CONSUMER_KEY
# - DISCOGS_CONSUMER_SECRET
# - DISCOGS_OAUTH_TOKEN_SECRET

